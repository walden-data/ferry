[build-system]
requires = ["maturin>=1.9.4,<2"]
build-backend = "maturin"

[project]
name = "ferry-core"
requires-python = ">=3.9"

[tool.maturin]
module-name = "ferry._native"
features = ["pyo3/extension-module"]
